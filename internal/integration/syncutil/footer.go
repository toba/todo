package syncutil

// SyncFooter is the attribution footer appended to issues synced to external
// services (GitHub, ClickUp) so viewers know the issue is managed externally.
const SyncFooter = `\[⚠️ This issue is managed by [todo](https://github.com/toba/todo). Edits made here will be overwritten.\]`
